# Caddy Reverse Proxy Configuration for PR Preview Environments
# This is the base Caddyfile. Dynamic routes are added via the admin API.

{
	admin 0.0.0.0:2019
	auto_https off
}

# Define a minimal HTTP server so the `apps/http` tree exists in the Admin API.
# IMPORTANT: This does NOT add any match-all 404/response that would shadow dynamic PR routes.
:80 {
	@noop {
		path /__caddy_noop__
	}
	respond @noop "ok" 200
}



