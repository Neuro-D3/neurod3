# Additional Python dependencies for the Airflow containers.
# Leave this file empty if you don't need extra Python packages beyond what
# the base apache/airflow image already provides.

# GCS remote task logging (staging/GCP). Provides the gs:// log handler so
# Airflow uploads task logs to Cloud Storage and streams them back into the UI.
# If a build hits a version conflict with apache-airflow 3.1.5, install with the
# official constraints file or pin a compatible provider version.
apache-airflow-providers-google

# fsspec GCS backend for utils/bucket.py (find_reuse cache access). Declared
# explicitly rather than relying on it arriving transitively via the Google
# provider, so the bucket module's gs:// reads/writes don't depend on provider
# packaging. Pulls fsspec as a dependency.
gcsfs
